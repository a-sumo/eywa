name = "swarmline-mcp"
main = "src/index.ts"
compatibility_date = "2025-01-13"
compatibility_flags = ["nodejs_compat"]

# The worker needs the service_role key (server-side only, bypasses RLS).
# Set it via: wrangler secret put SUPABASE_KEY
# NEVER put the service_role key in this file or any client code.
[vars]
SUPABASE_URL = "https://beknjtxysmznenkotjvv.snapcloud.dev"
